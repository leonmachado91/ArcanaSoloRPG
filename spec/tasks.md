- [x] Atualizar o `tailwind.config.ts`, `index.css` e o README para documentar novos tokens Arcana (cores, sombras, tipografia) e criar o `AppShell` que encapsula `AppContent`, toasts e overlays.
- [x] Migrar `GlobalOverlays` e cabeçalhos das telas para o novo shell, garantindo que os botões de Settings/DevLog funcionem em todas as rotas e respeitem o novo App Shell e seus tokens Arcana.
- [x] Reestruturar `GameRoomScreen` em subcomponentes (`GameRoomHeader`, `GameRoomLayout`) e introduzir painéis responsivos (drawer lateral em mobile) para ficha do jogador e NPCs.
- [ ] Implementar um "Command Bar" inferior substituindo o botão flutuante, expondo ações principais (Iniciar/Encerrar jogo, OFF, ajuda) e conectando-o às stores existentes.
- [ ] Criar o componente `Composer` baseado em `Textarea` e substituir o `<textarea>` manual do `PlayerInputBar`, adicionando contador, atalhos de teclado e suporte a anexos/imagens.
- [ ] Atualizar `Select`, `Toggle`, `NumberStepper`, `Button` e demais controles para obedecer tokens, estados de foco, navegação por teclado e mensagens de erro padrão.
- [ ] Refatorar `ChatDisplay`, `ChatMessage` e `SystemMessage` para suportar virtualização, agrupamento por autor, filtros (Jogador/Mestre/Sistema/RAW) e timeline de turnos/cenas.
- [ ] Transformar o modo RAW em um painel lateral colapsável compartilhado com o Dev Log, exibindo instruções do sistema, tool calls e busca textual.
- [ ] Extrair a lógica de rolagens/persistência de `useGameActions` para um orquestrador puro, adicionar testes de unidade e reduzir efeitos colaterais nos hooks React.
- [ ] Implementar uma fila observável em `useCharacterImageManager` (upload + geração), conectando feedback visual no Game Room e registrando eventos no `devLogStore`.
- [ ] Ajustar `narratorService` e `gameMasterService` para lidar com prompts ausentes via fallback, registrar warnings e atualizar o Dev Log com métricas de IA.
- [ ] Atualizar `spec/brief/07_ui_and_navigation.md` (ou documento correlato) com o novo design system, padrões de interação e checklist de acessibilidade.
